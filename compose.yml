# 本地开发使用。
# 只启动 Redis，PostgreSQL 直接使用本机安装的实例。
# 启动命令：docker compose up -d
services:
  redis:
    image: redis:8.10.0-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
