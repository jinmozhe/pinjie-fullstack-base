# 母版开发总结与一致性收尾计划

## 当前状态和结果

- 状态：已结束
- 结果：生产边界、OpenAPI 字段说明、TypeScript 依赖图、数据库迁移恢复和文档漂移已修复，三端质量、容器、文件日志和桌面/移动 Browser E2E 已完成验收

## 关联需求

- `BASE-GEN-001`、`BASE-GEN-002`、`BASE-GEN-003`、`BASE-GEN-004`、`BASE-GEN-005`、`BASE-GEN-006`、`BASE-GEN-007`、`BASE-GEN-008`
- `BASE-BE-001`、`BASE-BE-002`、`BASE-BE-003`、`BASE-BE-004`、`BASE-BE-005`、`BASE-BE-006`、`BASE-BE-007`、`BASE-BE-008`、`BASE-BE-009`
- `BASE-ADMIN-001`、`BASE-ADMIN-002`、`BASE-ADMIN-003`、`BASE-ADMIN-004`、`BASE-ADMIN-005`、`BASE-ADMIN-006`
- `BASE-WEB-001`、`BASE-WEB-002`、`BASE-WEB-003`、`BASE-WEB-004`、`BASE-WEB-005`、`BASE-WEB-006`、`BASE-WEB-007`
- `BASE-API-001`、`BASE-API-002`、`BASE-API-003`、`BASE-API-004`、`BASE-API-005`
- `BASE-DB-001`、`BASE-DB-002`、`BASE-DB-003`、`BASE-DB-004`、`BASE-DB-005`
- `BASE-OPS-001`、`BASE-OPS-002`、`BASE-OPS-003`、`BASE-OPS-004`、`BASE-OPS-005`、`BASE-OPS-006`、`BASE-OPS-007`、`BASE-OPS-008`、`BASE-OPS-009`、`BASE-OPS-010`、`BASE-OPS-011`
- `BASE-SEC-001`、`BASE-SEC-002`、`BASE-SEC-003`、`BASE-SEC-004`、`BASE-SEC-005`、`BASE-SEC-006`
- `BASE-QUAL-001`、`BASE-QUAL-002`、`BASE-QUAL-003`、`BASE-QUAL-004`
- `BASE-DOC-001`、`BASE-DOC-002`、`BASE-DOC-003`、`BASE-DOC-004`、`BASE-DOC-005`、`BASE-DOC-006`

## 背景

2026-08-21 对仓库规则、文档、计划、源码、OpenAPI、生成客户端、数据库迁移、依赖、容器、GitHub Actions 和质量门禁完成全量只读审计。审计确认三个应用和通用业务核心能力已经形成可继续开发的母版主体，但生产 Compose、Backend 文件日志、OpenAPI 字段说明、数据库迁移实证、恢复演练、前端依赖图门禁和当前性文档仍存在未闭环事项。

本计划统一承接这些问题，目标是让代码、配置、契约、迁移、运维步骤、计划状态和当前事实重新对应，并依据 `docs/PROJECT_REQUIREMENTS.md` 的 13 条母版完成验收给出可核验结论。

## 目标

1. 修复 PostgreSQL 18 生产数据卷路径和 Backend 生产文件日志权限风险。
2. 为全部公开 OpenAPI Schema 字段提供准确中文说明，并增加自动防漂移测试。
3. 让根 README、三端 README、项目结构、依赖白名单、计划状态、索引和 Changelog 与当前实现一致。
4. 验证 Alembic 空库升级、已有库升级和重复升级，并让本地开发数据库达到当前 head。
5. 提供可执行且具备防误连保护的 PostgreSQL 备份恢复演练入口，并保存本次专用测试库演练结果。
6. 补齐 1Panel 单机生产运行手册，明确部署等级、环境变量、迁移、健康检查、日志、备份、恢复和回滚边界。
7. 增强 TypeScript 前端依赖图门禁，覆盖跨应用引用、Feature 内部越界、循环依赖和可解析的动态导入。
8. 在真实 PostgreSQL、Redis 和三端应用环境中完成 Backend 集成测试、容器检查与桌面和移动端 Browser E2E。
9. 逐条回写 67 项 `BASE-*` 需求追踪和 13 条母版完成验收结果，不把未执行项记录成通过。

## 非目标

- 不增加 CMS、电商、博客等具体业务能力。
- 不修改已经确认的 Browser E2E 人工触发和镜像发布解耦决策。
- 不发布 GHCR 镜像，不创建 Tag、Release，不执行生产部署或生产数据库操作。
- 不读取、输出或提交真实 `.env`、密钥、令牌和数据库内容。
- 不删除、移动或重命名既有计划、ADR、迁移和项目文档。
- 不用本次收尾替代派生项目根据业务风险定义 SLI、SLO、错误预算、RTO 和 RPO 的责任。

## 现状分析

- `compose.prod.yml` 使用 PostgreSQL 18.4，却把命名卷挂载到 PostgreSQL 17 及以下使用的 `/var/lib/postgresql/data`。
- Backend 文件日志默认开启并创建相对路径 `logs/`；生产镜像以 UID 10001 运行，`/app` 没有可写目录，生产配置示例也没有显式关闭文件日志。
- 根 OpenAPI 的接口摘要和响应说明已中文化，但绝大多数 Schema 字段没有 `description`，与 `BASE-API-001` 和 Changelog 声明冲突。
- Alembic head 为 `20260820_01`，当前本机开发数据库停在 `20260815_01`；最新迁移缺少本机空库和已有库完整验证。
- Backend 和 Web README 仍描述阶段 B，Web README 还包含未安装依赖和具体商品页面假设。
- 项目结构文档和索引记录 290 个文件，审计时实际为 294 个，实施本计划后文件数还会继续变化。
- `pnpm-workspace.yaml` 实际允许 6 个依赖执行构建脚本，文档和 Changelog 只记录 `esbuild` 与 `sharp`。
- `plans/2026-08-20_GitHub CI线上失败修复计划.md` 与索引状态冲突，另外两份已结束计划保留未勾选验证和真实环境缺口。
- 备份恢复具有原则文档，但没有母版内的专用演练脚本和本次恢复实证；1Panel 生产手册仍标记为待补充。
- Backend 已有 import-linter 合同，现有前端边界脚本仍以文本扫描为主，不能完整证明循环依赖和动态导入边界。
- PRD 共 67 项 `BASE-*`；审计时有 5 项缺少计划级引用，本计划将补齐追踪入口并明确母版与派生项目责任。

## 方案设计

### 生产运行边界

1. 按 PostgreSQL 18 官方镜像目录约定，把生产命名卷挂载到 `/var/lib/postgresql`。
2. 生产 Compose 默认只输出标准错误流，对 Backend 和 request-log-consumer 显式设置 `LOG_FILE_ENABLED=false`。
3. 保留现有文件日志开关、路径、轮转、保留和压缩能力。需要文件落盘的部署必须配置明确的可写挂载和路径，运维文档给出受控示例。
4. 增加 Compose 配置检查或等价自动测试，防止 PostgreSQL 卷路径和生产日志策略再次漂移。

### OpenAPI 字段说明

1. 在 Backend Pydantic Schema 源码中为公开字段维护准确中文 `description`，不手工编辑 `openapi.json` 或生成客户端。
2. 对公共包装模型和重复语义复用明确的字段元数据，避免维护多份含义不一致的说明。
3. 增加 OpenAPI 测试，要求公开 Schema 属性都具有非空中文说明，同时允许程序标识继续使用英文。
4. 依次导出根 OpenAPI、生成 API Client，再验证 Admin 和 Web 类型、测试与构建。

### 数据库和恢复演练

1. 复核最新 revision 的升级、降级边界和幂等行为，不改写既有迁移历史。
2. 使用独立且名称以 `_test` 结尾的临时数据库验证空库升级、已有库升级和重复升级。
3. 在取得数据库操作授权后，把当前本地开发数据库升级到 head，仅执行已提交 Alembic revision。
4. 新增 PowerShell 备份恢复演练脚本。脚本必须校验环境、数据库名、源库和目标库隔离、工具存在性及确认参数，禁止指向生产库。
5. 演练使用专用临时 `_test` 数据库，验证 `pg_dump`、`pg_restore`、结构版本和基础数据一致性，结束后按授权清理临时库。

### 文档和治理同步

1. 更新根和三端 README 的当前能力、技术栈、启动命令和业务中立边界。
2. 重新生成项目结构清单和最终文件数，登记本计划新增文件。
3. 根据 Git 历史、锁文件和实际安装需求说明 6 项 `allowBuilds` 条目的来源与必要性，无法证明必要的条目不直接保留为既成事实。
4. 修正 Backend 规则和历史 ADR 中 `uv.lock` 尚未生成的过期当前性表述，同时保留 ADR 的历史决策语境。
5. 对计划状态冲突进行证据化收敛。已由后续验证覆盖的项目写明依据，仍未验证的项目保留为剩余问题。
6. 新增 1Panel 单机生产运行手册并登记到 `docs/README.md`，移除“待补充”状态。
7. 为历史原始方案增加醒目的历史资料边界，当前事实统一指向现行 ADR、架构和项目结构文档。
8. 更新 `.agents/agents-index.md` 和 `CHANGELOG.md`，只记录本次实际完成并验证的事实。

### 模块边界门禁

1. 保留现有 PowerShell 静态边界检查和 Backend import-linter 合同。
2. 优先使用仓库现有 TypeScript 编译能力构建 Admin、Web 的静态导入图；如果无法稳定覆盖，则评审并固定一个成熟依赖图工具作为开发依赖。
3. 新门禁检查应用间直接引用、Feature 内部越界、循环依赖和可静态解析的动态导入，并纳入根 `check:boundaries`。
4. 为门禁增加正反例，确保合法公开入口通过，越界、循环和动态导入违规失败。

### 最终验收

1. 启动项目约定的 PostgreSQL、Redis、Backend、Admin 和 Web 测试环境。
2. 运行治理、Markdown、文本卫生、Backend 全量质量门禁、两端质量门禁、OpenAPI 生成漂移、Docker 构建和 Compose 健康检查。
3. 运行桌面和移动端 Browser E2E、axe、横向溢出和关键认证管理流程。
4. 清理本次启动且已核对归属的服务、端口、临时数据库、测试标签和构建缓存。
5. 在计划中逐条记录 13 项验收结论，区分通过、失败、未执行和派生项目责任。

## 全栈影响矩阵

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 涉及 | OpenAPI 字段说明、日志生产边界、测试与迁移验证 | Python、uv、PostgreSQL、Redis | Ruff、Mypy、import-linter、pytest、OpenAPI |
| Admin | 涉及 | 共享契约消费、README 和依赖图门禁 | pnpm、生成 API Client | Lint、typecheck、Vitest、build、E2E |
| Web | 涉及 | 共享契约消费、README 和依赖图门禁 | pnpm、生成 API Client | Lint、typecheck、Vitest、build、E2E |
| API Client | 涉及 | 由更新后的根 OpenAPI 重新生成 | Backend 契约完成 | 生成无漂移、两端消费验证 |
| Database | 涉及 | 最新迁移、空库和已有库升级、备份恢复演练 | 独立 `_test` 数据库和授权 | Alembic check、upgrade、restore 校验 |
| Deployment | 涉及 | PostgreSQL 卷、日志策略、1Panel 手册、容器和健康检查 | Docker Desktop、三个应用镜像 | Compose config、build、health、rollback 文档复核 |
| Documentation | 涉及 | README、架构、运维、索引、计划、Changelog 和需求追踪 | 实现与验证最终事实 | markdownlint、链接、文本卫生、全量交叉核对 |

## 实施顺序

1. 创建本计划并登记到全项目索引，等待用户确认。
2. 修复生产 Compose 和日志运行边界，增加对应自动检查。
3. 补齐 Backend Schema 中文字段说明，导出 OpenAPI 并重新生成 API Client。
4. 增强 TypeScript 依赖图边界门禁和正反例。
5. 新增备份恢复演练入口和 1Panel 生产运行手册。
6. 同步 README、结构清单、依赖白名单、历史边界、计划状态和索引。
7. 在取得授权后执行本地数据库升级、专用测试库迁移与恢复演练。
8. 运行全仓质量、容器和 Browser E2E 验收。
9. 回写计划结果、13 条验收结论、索引和 Changelog，清理本次验证产物。

## 影响文件

- `compose.prod.yml`
- `apps/backend/.env.example`
- `apps/backend/app/core/logging.py`
- `apps/backend/app/**/schemas.py`
- `apps/backend/tests/`
- `apps/backend/AGENTS.md`
- `apps/backend/README.md`
- `apps/admin/README.md`
- `apps/web/README.md`
- `openapi.json`
- `packages/api-client/src/`
- `pnpm-workspace.yaml`
- `package.json`
- `pnpm-lock.yaml`
- `scripts/ci/`
- `scripts/operations/`
- `docs/README.md`
- `docs/PROJECT_REQUIREMENTS.md`
- `docs/architecture/project-structure.md`
- `docs/architecture/module-boundaries.md`
- `docs/architecture/全栈Monorepo架构规划原始方案.md`
- `docs/operations/container-build-and-run.md`
- `docs/operations/database-backup-restore.md`
- `docs/operations/environment-variables-and-backend-local-run.md`
- `docs/operations/1panel-production-runbook.md`
- `CHANGELOG.md`
- `.agents/agents-index.md`
- 发生状态冲突或需要回写验证结果的既有 `plans/*.md`
- `plans/2026-08-21_母版开发总结与一致性收尾计划.md`

实际影响文件以实施差异为准。没有证据需要变更的候选文件保持不动。

## 风险与回滚

- 风险：PostgreSQL 卷挂载修正会改变新容器的数据目录挂载方式，已有生产实例不能直接套用而不迁移数据。
- 控制：本次只修改母版配置和文档，不操作生产卷；手册明确新部署与既有部署迁移边界。
- 风险：大量 Schema 描述调整可能改变生成客户端注释并产生较大差异。
- 控制：不改变路径、字段名、类型、必填性和响应语义；使用契约差异检查确认只有文档元数据变化。
- 风险：依赖图门禁可能暴露既有循环或引入新的开发依赖。
- 控制：先盘点现有图，再区分真实违规与工具配置；新增依赖必须固定版本、审计来源并通过锁文件和安全门禁。
- 风险：数据库升级和恢复演练涉及创建、写入和删除临时数据库。
- 控制：仅使用名称以 `_test` 结尾的专用数据库，校验与开发和生产库隔离，并在执行前取得明确授权。
- 回滚：源码和文档改动可按文件差异回退；数据库只执行既有升级迁移，临时演练库独立创建；不对生产环境执行回滚。

## 验证清单

- [x] 最终文档修改后的 `pnpm lint:md` 通过。
- [x] 最终文档修改后的 `pnpm check:governance` 通过。
- [x] `pnpm check:workspace` 已通过，Backend、Admin、Web 均为 `ready`。
- [x] `pnpm check:boundaries` 及新增 TypeScript 正反例已通过，44 个文件、61 条依赖边通过检查。
- [x] Backend 编译、应用导入、Ruff、格式、Mypy 和 import-linter 通过。
- [x] Backend 全量 105 项 pytest 通过，真实 PostgreSQL、Redis 测试没有跳过，覆盖率 90.55%。
- [x] 本地开发库增量升级、专用 PostgreSQL 18.4 空库升级、重复升级和 `alembic check` 通过。
- [x] PostgreSQL 备份恢复演练在独立 `_test` 数据库通过。
- [x] OpenAPI 238 个公开 Schema 字段均具有非空中文说明。
- [x] 根 OpenAPI 和 API Client 已重新生成，非文档契约结构与修改前一致。
- [x] Admin typecheck、lint、17 项 Vitest、覆盖率和 production build 通过。
- [x] Web typecheck、lint、18 项 Vitest、覆盖率和 production build 通过。
- [x] 三端 `linux/amd64` Docker 镜像构建、非 Root 用户和真实健康检查通过。
- [x] 桌面和移动端 Browser E2E、axe 和横向溢出检查通过，8 项适用测试通过、8 项跨项目测试按设计跳过。
- [x] README、PRD、索引、ADR、架构、运维、计划和 Changelog 已同步并通过最终交叉检查。
- [x] 67 项 `BASE-*` 已在本计划显式登记，并在下方按母版与派生责任分组。
- [x] 13 条母版完成验收已逐项回写，全部计划内验收项具有最终结论。
- [x] 临时恢复库、容器、网络、镜像、测试缓存、构建产物、Playwright 运行时缓存、进程和端口已清理。
- [x] 根 OpenAPI 和 API Client 最终无漂移，`git diff --check` 和工作区差异复核通过。

## 待确认问题

- 无。

## 用户确认记录

- 2026-08-21：用户要求按照全量审计报告的建议进行实现。由于本计划包含数据库操作、部署配置和可能的依赖调整，等待用户对具体范围再次确认后进入实施。
- 2026-08-21：用户确认三项实施边界，授权生产 Compose 默认关闭文件日志、本地数据库升级与专用 `_test` 数据库演练、验证服务启停，以及必要时增加经审查并固定版本的依赖图开发工具。
- 2026-08-21：用户要求先完成全部文档更新，暂停后续 Web、容器和 Browser E2E 测试。
- 2026-08-21：用户确认恢复并执行全部剩余验收，授权 Admin/Web 质量门禁、契约漂移检查、三端容器、桌面和移动 Browser E2E、文件日志观察及本轮产物清理；明确禁止提交、推送、发布和生产操作。

## 需求追踪结论

| 需求组 | 编号 | 当前结论 | 事实入口 |
| --- | --- | --- | --- |
| 通用原则 | `BASE-GEN-001` 至 `BASE-GEN-008` | 母版、派生、计划、受控兼容和机械边界已建立，最终跨栈验收通过 | 根 `AGENTS.md`、PRD、模块边界、计划索引 |
| Backend | `BASE-BE-001` 至 `BASE-BE-009` | 通用能力已实现，105 项真实依赖测试与架构合同通过 | `apps/backend/app/`、`apps/backend/tests/`、Backend 规则 |
| Admin | `BASE-ADMIN-001` 至 `BASE-ADMIN-006` | 登录、RBAC 和通用管理工作台已实现，完整质量、容器和桌面/移动跨栈验收通过 | `apps/admin/src/`、Admin README、ADR 0011 |
| Web | `BASE-WEB-001` 至 `BASE-WEB-007` | 认证和用户中心已实现，完整质量、容器和桌面/移动跨栈验收通过 | `apps/web/src/`、Web README |
| API 与共享包 | `BASE-API-001` 至 `BASE-API-005` | 根契约、生成客户端、中文字段说明和消费者边界已实现 | 根 `openapi.json`、`packages/api-client/src/` |
| Database | `BASE-DB-001` 至 `BASE-DB-005` | PostgreSQL、Alembic、事务边界与恢复工具已实现，迁移恢复实证通过；具体业务不变量由派生项目定义 | Models、Alembic、恢复手册与脚本 |
| 运维与部署 | `BASE-OPS-001` 至 `BASE-OPS-011` | 本地、锁文件、CI、不可变发布、单机部署和恢复边界已建立，三端镜像和 E2E 复验通过；SLI、SLO、RTO、RPO 由派生项目定义 | 运维手册、工作流、Compose、ADR 0008 |
| 安全 | `BASE-SEC-001` 至 `BASE-SEC-006` | 服务端认证授权、审计、秘密与供应链边界已实现，非 Root 容器和当前跨栈安全回归通过 | SECURITY、认证架构、CI 工作流 |
| 质量 | `BASE-QUAL-001` 至 `BASE-QUAL-004` | Fail Closed、保留既有能力、UTF-8 和显式失败契约已落门禁，全部计划内质量检查通过 | 规则、错误模型、治理脚本 |
| 文档治理 | `BASE-DOC-001` 至 `BASE-DOC-006` | 索引、PRD、ADR、计划保护、docs 单一来源和 Changelog 已建立，本次当前事实已同步 | `.agents/agents-index.md`、`docs/README.md`、`CHANGELOG.md` |

## 母版完成验收矩阵

| 序号 | 状态 | 结论与证据 |
| --- | --- | --- |
| 1 | 已满足 | 67 项需求已全部追踪，三端质量、容器和桌面/移动跨栈验收完成；派生项目责任仍按 PRD 独立落实。 |
| 2 | 已满足 | 三端均为 `ready`；Backend 105 项 pytest、Admin 17 项 Vitest、Web 18 项 Vitest 及各自完整质量门禁通过。 |
| 3 | 已满足 | OpenAPI 可导出，API Client 已生成且最终无漂移，Admin/Web 均消费共享包并通过 production build。 |
| 4 | 已满足 | 本地开发库已到 `20260820_01`，PostgreSQL 18.4 空库、重复升级、`alembic check` 和防误连反例通过。 |
| 5 | 已满足 | 三端 `linux/amd64` 镜像构建、非 Root 用户和真实健康检查通过。 |
| 6 | 已满足 | Backend 真实 PostgreSQL/Redis 测试通过，Web/Admin 桌面和移动端关键跨栈旅程通过。 |
| 7 | 已满足 | 桌面和移动端关键页面通过 axe、文字可见性和横向溢出检查。 |
| 8 | 已满足 | README、PRD、索引、ADR、架构、运维、计划和 Changelog 已按当前代码同步，并通过 Markdown、文本卫生和交叉复核。 |
| 9 | 已满足 | 根 `pnpm-lock.yaml` 和 `apps/backend/uv.lock` 均已生成，依赖职责和复现命令明确。 |
| 10 | 已满足 | 未发现真实秘密进入受管差异，临时数据库、容器、网络、镜像、测试缓存、构建产物和进程端口均已清理。 |
| 11 | 已满足 | 三态、生成契约、生产配置和架构正反例门禁通过，最终 OpenAPI/API Client 无漂移。 |
| 12 | 机制满足 | 工作流要求 Commit SHA、SBOM、来源证明和完整 digest；本计划未获授权发布镜像或生产部署，不生成本次生产记录。 |
| 13 | 已满足 | 模块依赖、生产配置、恢复演练、三端容器和桌面/移动 E2E 通过，单机部署等级已文档化。 |

当前结论：13 条母版完成验收均已形成可核验证据，其中不可变发布为机制验收且本计划按授权未实际发布或部署。母版主体、核心通用能力和治理已达到稳定派生基线，本计划标记为“已结束”。

## 实施结果

- 生产 Compose 已修正 PostgreSQL 18 卷路径，Backend 和请求日志消费者默认关闭文件日志，并新增生产配置正反例门禁。
- OpenAPI 238 个公开 Schema 字段已补齐中文说明，导出脚本不再受本机运行配置污染，根契约和 API Client 已重新生成。
- TypeScript Compiler API 依赖图门禁已覆盖跨应用引用、Feature 内部越界、循环依赖和可解析动态导入，并具有正反例。
- 本地开发数据库已升级到 `20260820_01`；专用 PostgreSQL 18.4 完成空库、重复升级和恢复演练。
- 恢复演练结果：revision `20260820_01`，14 张 public 表、1 行数据、130 个约束，备份 37,897 字节，耗时 2.58 秒；恢复库、临时 dump、容器和随机端口均已清理。
- 根与三端 README、项目结构、技术栈、依赖白名单、历史边界、环境变量、容器、恢复、1Panel、文档索引和当前状态文档已同步。
- Admin typecheck、lint、17 项 Vitest、覆盖率和 production build 通过；Browser E2E 发现并修复五个受保护 lazy route 页面缺少默认导出导致的 production React 306 错误。
- Web typecheck、lint、18 项 Vitest、覆盖率和 production build 通过；新增主动退出中止在途用户/会话查询的回归保护，避免迟到 401 覆盖预期登录跳转。
- Backend、Web、Admin 三张 `linux/amd64` 镜像完成构建，均以非 Root 用户运行并达到真实 `healthy`；独立非 Root Backend 容器的文件日志创建、权限和脱敏内容人工观察通过。
- Playwright 桌面与移动矩阵完成 8 项适用 E2E，8 项跨项目用例按设计跳过；关键旅程、axe 和横向溢出检查通过。
- OpenAPI/API Client 最终无漂移；临时数据库、容器、网络、镜像、测试报告、缓存、构建产物、Playwright 运行时、进程和端口已清理。

## 剩余问题

- 本计划无未完成验收项。GHCR 发布、Tag、Release、生产部署、生产数据库操作、Git 提交和推送均按授权边界未执行，不属于本计划验收缺口。
- Playwright 完整 Chromium 包下载受本机 TLS 连接失败影响，本轮使用 Playwright 官方同版本 headless shell 完成自动化验收；不影响当前 E2E 结果，但没有新增 headed browser 人工视觉走查证据。
