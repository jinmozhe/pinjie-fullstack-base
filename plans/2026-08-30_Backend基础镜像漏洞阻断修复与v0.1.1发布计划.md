# Backend 基础镜像漏洞阻断修复与 v0.1.1 发布计划

## 当前状态和结果

- 状态：实施中
- 结果：不适用

## 关联需求

- `BASE-SEC-005`：保持依赖、容器、SBOM 和构建来源证明的分层安全门禁。
- `BASE-QUAL-004`：镜像扫描失败必须明确阻断，禁止关闭门禁或把候选镜像表述为可发布制品。
- `BASE-DOC-004`：通过全栈计划记录修复、验证和发布证据。

## 背景

`v0.1.0` 对应提交 `230de3cda8ee7c46c144cb254d4f05e3d73dffdd` 的完整远端验证已经通过，但 `Publish Images` Run `33267308587` 在 Backend 候选镜像的 Trivy 扫描阶段失败。扫描发现 Debian 39 个 High 和 Python 2 个 High，Critical 为 0；Web 与 Admin 矩阵任务随后因 `fail-fast` 被取消，最终不可变标签没有发布。

Backend Dockerfile 固定的 `python:3.14.7-slim-trixie` Linux amd64 digest 已落后于 Docker Hub 当前同标签摘要。漏洞包未出现在项目 `pyproject.toml` 或 `uv.lock` 中，修复应从固定基础镜像版本入手，并由新的候选镜像扫描证明结果。

## 目标

1. 更新 Backend 的固定 Python 基础镜像 digest，同时保留标准 CPython 3.14.7 slim-trixie 技术基线。
2. 保持 Trivy High/Critical Fail Closed、SBOM、provenance 和同 SHA 完整验证门禁不变。
3. 通过功能分支、Pull Request 和 rebase 合并把修复交付到 `main`。
4. 为合并后的新提交创建不可变 `v0.1.1` Tag 和正式 GitHub Release。
5. 对同一新提交完成远端完整验证并成功发布 Backend、Web、Admin 三张 GHCR 镜像，取得三个固定 digest。

## 非目标

- 不移动、覆盖或删除现有 `v0.1.0` Tag 和 GitHub Release。
- 不降低、跳过或增加 Trivy 漏洞豁免。
- 不触发生产部署，不连接生产服务器，不修改生产配置或 Secret。
- 不修改应用业务逻辑、公开 API、数据库模型、迁移、Admin 或 Web 功能。

## 现状分析

- 完整验证 Run `33266778572` 已证明 `v0.1.0` 源码通过 pytest、Vitest、production build 和 Playwright，但证据只能绑定原提交。
- Backend 候选镜像扫描失败是发布根因；Web 与 Admin 的失败状态来自矩阵取消，不能解释为各自独立漏洞或构建缺陷。
- 旧基础镜像 digest 为 `sha256:ce40764625a4ff50df3548277632e7f96c4e77fe75fa848aae9885476e7df5a4`。
- Docker Hub 官方 API 返回当前 Linux amd64 digest 为 `sha256:5b945726c4d7ffe6595dc7498ad17fd0397e373c5181c24819a7028e8b1276dc`。
- 基础镜像 digest 更新会产生新 Commit SHA，因此必须重新获得四项 Push 工作流和同 SHA 完整验证 Artifact。

## 方案设计

1. 将 Backend builder 与 runtime 阶段统一固定到 Docker Hub 当前的 Python 3.14.7 slim-trixie Linux amd64 digest。
2. 运行仓库轻量门禁、Markdown lint、工作区与边界检查，确认代码和文档无漂移。
3. 在 `pre-pr` 收尾中同步本计划、计划索引、项目索引、发布运维当前事实和 Changelog。
4. 提交并推送功能分支，创建目标为 `main` 的 Pull Request，等待必需检查后以 rebase 方式合并。
5. 等待合并提交对应的 Governance、Backend、Frontend 和 Security 四项 Push 工作流全部成功。
6. 创建 `v0.1.1` Tag 和正式 GitHub Release，Tag 固定指向合并提交。
7. 人工触发同 SHA `CI - Full Validation`，成功后触发 `Publish Images`。
8. 若候选镜像扫描仍失败，保留安全门禁并依据新日志继续修复，通过新的提交和完整验证重走发布链路。

## 全栈影响矩阵

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 涉及 | 更新 builder 与 runtime 固定基础镜像 digest | Docker Hub 官方镜像摘要 | Backend 轻量门禁、远端完整验证、Trivy |
| Admin | 不涉及 | 不修改源码；参与最终镜像发布 | 同 SHA 完整验证 | Vitest、production build、Playwright、Trivy |
| Web | 不涉及 | 不修改源码；参与最终镜像发布 | 同 SHA 完整验证 | Vitest、production build、Playwright、Trivy |
| API Client | 不涉及 | 无公开 API 变化 | 无 | 生成漂移门禁 |
| Database | 不涉及 | 无模型或迁移变化 | 无 | 契约和工作区检查 |
| Deployment | 涉及 | 修复镜像发布输入并生成三个不可变 digest | 四项 Push 工作流和完整验证 Artifact | Publish Images 成功 Summary |
| Documentation | 涉及 | 同步计划、索引、运维说明和 Changelog | 实际验证与发布结果 | `pnpm lint:md`、文本卫生检查 |

## 实施顺序

1. 建立计划与活动索引。
2. 更新 Backend 固定基础镜像 digest。
3. 执行本地轻量门禁和文档同步。
4. 提交、推送、创建 PR、等待检查并 rebase 合并。
5. 等待合并提交的四项 Push 工作流。
6. 创建 `v0.1.1` Tag 和 GitHub Release。
7. 触发并等待同 SHA 完整验证。
8. 触发并等待 GHCR 镜像发布，核对三个 digest。
9. 回写计划结果和最终发布证据；若需要回写产生新的发布后提交，只记录发布事实，不移动 `v0.1.1`。

## 影响文件

- `apps/backend/Dockerfile`
- `plans/2026-08-30_Backend基础镜像漏洞阻断修复与v0.1.1发布计划.md`
- `plans/INDEX.md`
- `PROJECT_INDEX.md`
- `docs/operations/release-and-rollback.md`
- `CHANGELOG.md`

## 风险与回滚

- 新基础镜像可能仍包含 High/Critical 漏洞。回滚方式是停止发布并依据 Trivy 证据选择后续修复版本，禁止关闭扫描。
- 基础镜像更新可能改变系统库行为。由完整 pytest、production build 和 Playwright 验证应用行为，由三镜像扫描验证供应链门禁。
- 已发布 Tag 不允许移动。合并前可通过普通反向提交回滚；`v0.1.1` 发布后发现问题时创建后续修复版本，不覆盖现有 Tag、Release 或镜像标签。
- GHCR 候选内容可能在最终标签创建前可见。只有整个 `Publish Images` 工作流成功且 Summary 给出三个 digest 后才能用于部署。

## 验证清单

- [x] Dockerfile 两个 Python 阶段使用同一完整新 digest。
- [x] `pnpm lint:md`
- [x] `pnpm check:workspace`
- [x] `pnpm check:boundaries`
- [x] Backend Ruff、格式、Mypy、导入边界、编译、应用导入和契约检查。
- [ ] Pull Request 必需检查全部通过并以 rebase 合并。
- [ ] 合并提交四项 Push 工作流全部成功。
- [ ] `v0.1.1` Tag 和 GitHub Release 指向同一完整 Commit SHA。
- [ ] 同 SHA 完整验证成功并生成未过期 Artifact。
- [ ] `Publish Images` 成功，Backend、Web、Admin 三路 Trivy 扫描通过。
- [ ] 取得三个 `sha256:<64 位十六进制>` digest。
- [ ] 本次未触发生产部署。

## 待确认问题

无。用户已明确授权创建计划后直接实施，以及完成 GHCR 镜像发布成功前所需的 Git、Tag、Release、完整验证、错误诊断和修复动作。

## 用户确认记录

- 2026-08-30：用户明确授权创建计划后直接实现，自动处理失败原因并修复，提交、推送及完成发布所需 Git 动作，创建正式 Tag 和 GitHub Release，执行完整验证与 GHCR 镜像发布，直到取得成功结果；生产部署由用户后续人工执行。

## 实施结果

实施中。

- Backend builder 与 runtime 已同步固定到 Python 3.14.7 slim-trixie 当前 Linux amd64 digest `sha256:5b945726c4d7ffe6595dc7498ad17fd0397e373c5181c24819a7028e8b1276dc`。
- `pnpm check:governance` 已通过，包括 Markdown、文档治理、工作区、依赖、模块边界、生产配置及全部正反例守卫。
- Backend Ruff、格式、Mypy、import-linter、源码编译、应用导入和 OpenAPI 导出均通过；API Client 重新生成后无契约漂移。
- 项目卫生文本检查覆盖 147 个文件且无错误；`.env.example` 的公开模板值产生 1 条既有启发式凭据告警，本次未修改该文件。
- 本地 Docker 构建在获取 Docker Hub OAuth Token 时因当前网络连接失败，尚未进入镜像层构建；该结果不作为容器验证证据，后续以 GitHub Runner 的构建和 Trivy 扫描为准。

## 剩余问题

- 待完成 PR 交付、`v0.1.1` Tag 与 Release、同 SHA 完整验证和 GHCR 三镜像发布。
