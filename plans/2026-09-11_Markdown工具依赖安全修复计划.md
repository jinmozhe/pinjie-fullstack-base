# Markdown 工具依赖安全修复计划

## 状态、目标与授权

- 当前状态：已结束
- 结束结果：已完成；smol-toml 安全版本、根锁文件和治理门禁已同步，PR #53 的 13 项必需检查通过并纳入本次 Git 交付
- 目标：修复 PR #53 线上 Node dependency audit 的 smol-toml 高危阻断，继续完成本次 git-sync。
- 关联需求：`BASE-SEC-005`、`BASE-OPS-002`、`BASE-OPS-008`、`BASE-QUAL-003`。
- 用户确认记录：2026-09-11 用户显式执行 `$git-sync`，授权交付中的依赖安全修复、轻量验证、追加提交、PR 合并与 main 同步；不包含重型测试、发布或部署。
- 待确认问题：无。

## 证据与方案

[线上失败日志](https://github.com/jinmozhe/pinjie-fullstack-base/actions/runs/34574849261/job/103184861250)报告根开发工具链 `markdownlint-cli2 0.23.2 → smol-toml 1.7.0` 命中 [GHSA-7w5x-hrqm-74c2](https://github.com/squirrelchat/smol-toml/security/advisories/GHSA-7w5x-hrqm-74c2)：`<=1.7.0` 存在畸形 TOML 导致拒绝服务的问题，首个修复版本为 1.7.1。

从 npm 官方元数据确认 1.7.1 发布于 2026-07-26，满足七天冷却期；要求 Node >=18，无运行时依赖及安装构建脚本。修复前固定的 markdownlint-cli2 0.23.2 精确依赖 1.7.0，因此在根 workspace 对 `markdownlint-cli2@0.23.2>smol-toml` 增加 1.7.1 的限定 override，由 pnpm 重新生成锁文件。保留 markdownlint 与 CLI 固定版本、规则配置、冷却、信任策略和构建白名单。

override 负责人为仓库维护者，2026-09-25 复核。后续上游 CLI 升级并直接依赖安全版本时，在同一升级计划内删除 override；删除前验证锁文件解析版本、Markdown 检查和线上 Security。

## 约束与全栈影响

| 来源 | 约束 | 落实与验证 |
| --- | --- | --- |
| 根 AGENTS、开发流程 17 与 18.1 节 | 自动修复与三次失败上限，保留恢复点 | 同一 PR 追加补丁；本项首次发现记为第 0 次，无修复后失败 |
| SECURITY 与 pnpm workspace | 唯一锁文件、七天冷却、有限 override | 仅替换根 Markdown 工具传递依赖，不手工改锁文件 |
| 开发流程 9.9 节 | Security 只在线上执行 | 本地不运行 audit 或扫描器，以 PR 复验确认漏洞门禁 |
| 计划与文档规则 | 记录真实范围和结果 | 原计划、两个索引、Changelog、PR 说明 |

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 不涉及 | Python 依赖与源码不变 | 无 | 原有 PR 轻量 CI |
| Admin | 不涉及 | 应用依赖和 UI 不变 | 无 | 原有 PR 轻量 CI |
| Web | 不涉及 | 应用依赖和 UI 不变 | 无 | 原有 PR 轻量 CI |
| API Client | 不涉及 | 契约及生成客户端不变 | 无 | 原有 PR 契约门禁 |
| Database | 不涉及 | 无迁移或数据写入 | 无 | 不执行数据库验证 |
| Deployment | 不涉及 | 无镜像发布、部署或生产变更 | 无 | 不执行构建与部署 |
| Documentation | 涉及 | 根 Markdown 工具依赖、计划、索引和 Changelog | 官方修复版本、pnpm store | 锁文件核对、lint:md、文本和文档门禁 |

## 实施与验证

1. 已登记本计划与两个索引，并精确修改 `pnpm-workspace.yaml`。
2. 已使用 npm 官方源和既有 `.pnpm-store` 生成根锁文件；差异仅包含 workspace override、`smol-toml` 版本与完整性哈希。宿主冻结安装完成，供应链策略核对通过。
3. 已运行 `pnpm lint:md`、`pnpm check:text`、`pnpm check:documents`、`pnpm check:dependencies`、`pnpm check:workspace` 和 `pnpm check:boundaries`。Markdown lint 检查 176 个文件无问题；TypeScript 边界检查覆盖 77 个文件、145 条边；实际安装版本为 `smol-toml@1.7.1`，旧版 1.7.0 不存在。本批不改变应用、边界或治理脚本。
4. 已提交同一 PR 的修复提交；线上 Security 与全部 13 项必需检查通过，本次 git-sync 继续完成 rebase 合并、分支清理及 main 同步。普通提交 SHA 不回写文档。

## 风险与回滚

- 这是根开发工具的解析器补丁，Markdown 检查与应用轻量 CI 不能代替完整业务验证；未授权的应用测试、构建、浏览器和数据库验证保持未执行。
- 不允许通过恢复漏洞版本、放宽审计或接受 High 风险完成合并。需要撤销补丁时保留安全阻断，不触及业务数据和部署。
- 当前源码已有 Git 提交与本次恢复记录，锁文件通过 pnpm 生成并逐项审查；`pnpm why smol-toml --recursive` 已确认仅存在 1.7.1。

## 实施结果与剩余问题

本地修复与轻量验证已完成：workspace override 和锁文件将 `markdownlint-cli2@0.23.2` 的 `smol-toml` 固定到 1.7.1，冻结安装、实际版本核对、Markdown、文本、文档、依赖、工作区和边界门禁均通过。PR #53 的 Node dependency audit、依赖漏洞扫描、Secret scan、Semgrep、Backend/Admin/Web quality、治理和 OpenAPI 等 13 项必需检查全部通过。高危漏洞首次发现为第 0 次；修复后未出现同一错误的复验失败。

剩余问题：无。Vitest、pytest、production build、Playwright、浏览器自动化、测试数据库验证、发布和部署按授权未执行。
