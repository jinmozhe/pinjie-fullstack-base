# CI 跨平台与 CodeQL 权限修复计划

## 当前状态和结果

- 状态：实施中
- 结果：无

## 关联需求

- 无。本计划修复既有 CI 配置缺陷，不改变产品能力或验收范围。

## 背景

GitHub Actions 中应用测试均已通过，但 `CI - Governance` 和 `Security` 工作流持续失败。已确认 Governance 失败发生在 Linux PowerShell 读取点文件时，Security 失败发生在私有仓库 CodeQL 上传结果阶段。两处失败均属于既有 CI 配置问题。

## 目标

1. 让文本卫生检查在 Linux PowerShell 中正确读取被 Git 跟踪的点文件。
2. 为 JavaScript/TypeScript 和 Python CodeQL Job 补齐读取 Actions 元数据所需权限。
3. 保持现有安全扫描、失败关闭策略和应用行为不变。
4. 完成仓库治理检查，并明确线上 GitHub Actions 复验所需的独立推送授权。

## 非目标

- 不修改 Backend、Admin、Web 或 API Client 源码。
- 不调整依赖版本、锁文件、数据库或部署配置。
- 不弱化 CodeQL、文本卫生检查或其他 CI 门禁。
- 不在本计划确认时自动提交、推送或触发线上工作流。

## 现状分析

- `scripts/ci/check-text-files.ps1` 使用 `Get-Item -LiteralPath` 获取文件信息；Linux PowerShell 对 `.agents/rules/.markdownlint.json` 等点文件需要显式包含隐藏项，当前 Governance Run 因找不到该文件而失败。
- `.github/workflows/security.yml` 的两个 CodeQL Job 只声明 `contents: read` 和 `security-events: write`；CodeQL 分析和 SARIF 生成完成后，请求 Actions Run 元数据时返回 `Resource not accessible by integration`。
- 本地 `main` 正确跟踪 `origin/main`，工作区在创建本计划前为干净状态。

## 方案设计

1. 将文本检查中的文件读取改为 `Get-Item -Force -LiteralPath $fullPath`，仅修复点文件读取，不改变扫描范围和编码规则。
2. 在 `codeql-javascript` 和 `codeql-python` 的 Job 级 `permissions` 中增加 `actions: read`，保留原有最小权限。
3. 运行文本检查、Markdown 检查、工作区治理和模块边界检查，复读配置与差异。
4. 本地修复完成后停止在提交前；提交、推送和线上 Actions 复验分别等待用户明确授权。

## 全栈影响矩阵

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 不涉及 | 不修改后端源码、依赖或契约 | 无 | Git 差异检查 |
| Admin | 不涉及 | 不修改 Admin 源码或依赖 | 无 | Git 差异检查 |
| Web | 不涉及 | 不修改 Web 源码或依赖 | 无 | Git 差异检查 |
| API Client | 不涉及 | 不修改 OpenAPI 或生成客户端 | 无 | Git 差异检查 |
| Database | 不涉及 | 不修改模型、迁移或数据 | 无 | Git 差异检查 |
| Deployment | 涉及 | 修复 Governance 脚本的 Linux 兼容性和 Security 工作流权限 | 用户确认计划 | 本地治理检查；推送后核验 GitHub Actions |
| Documentation | 涉及 | 维护计划、项目索引、项目结构和变更日志 | 配置修复事实 | `pnpm lint:md`、文档复读 |

## 实施顺序

1. 用户确认本计划后，更新状态为“实施中”并记录确认。
2. 修改 `scripts/ci/check-text-files.ps1` 的点文件读取参数。
3. 修改 `.github/workflows/security.yml` 两个 CodeQL Job 的权限。
4. 更新 `docs/architecture/project-structure.md`、`CHANGELOG.md`、本计划和 `.agents/agents-index.md`。
5. 运行文本、Markdown、工作区治理和模块边界检查。
6. 复读修改文件并检查 Git 差异、编码和末尾换行。
7. 本地交付后，等待用户分别授权提交和推送；推送后核验对应 GitHub Actions Run。

## 影响文件

- 修改：`scripts/ci/check-text-files.ps1`
- 修改：`.github/workflows/security.yml`
- 修改：`docs/architecture/project-structure.md`
- 修改：`CHANGELOG.md`
- 新增并持续更新：`plans/2026-08-16_CI跨平台与CodeQL权限修复计划.md`
- 修改：`.agents/agents-index.md`

## 风险与回滚

- 风险：`actions: read` 会让 CodeQL Job 的 `GITHUB_TOKEN` 能读取本仓库 Actions 元数据；权限为只读且限定在 Job 内。
- 风险：本地 Windows 验证无法完全替代 GitHub Ubuntu Runner，最终结论需要推送后观察线上工作流。
- 回滚：移除两个 Job 的 `actions: read` 可恢复原权限，但会重新触发当前 CodeQL 失败。
- 回滚：移除 `Get-Item` 的 `-Force` 可恢复原行为，但 Linux 点文件读取会重新失败。

## 验证清单

- [x] `pnpm check:text` 通过。
- [x] `pnpm lint:md` 通过。
- [x] `pnpm check:workspace` 通过。
- [x] `pnpm check:boundaries` 通过。
- [x] `pnpm check:guards` 通过。
- [x] 两个 CodeQL Job 均包含 `actions: read`、`contents: read` 和 `security-events: write`。
- [x] 文本检查仍覆盖原有扩展名、特殊文件名、UTF-8 BOM、UTF-8 有效性和末尾换行规则。
- [x] 修改文件均为 UTF-8 无 BOM 并保留末尾换行。
- [ ] 获得独立推送授权后，GitHub `CI - Governance` 和 `Security` 工作流通过。

## 待确认问题

- 无。用户确认本计划后即可实施本地修复；提交和推送仍需后续独立授权。

## 用户确认记录

- 2026-08-16：用户明确回复“确认执行”，授权按本计划实施本地 CI 配置修复和验证；提交、推送及线上工作流复验仍需独立授权。

## 实施结果

- 已将 `scripts/ci/check-text-files.ps1` 的文件信息读取改为带 `-Force` 的精确路径读取，修复 Linux PowerShell 无法获取 Git 点文件的问题，未改变文件枚举、扩展名过滤和文本卫生规则。
- 已在 JavaScript/TypeScript 和 Python CodeQL Job 的局部权限中增加 `actions: read`，保留 `contents: read` 和 `security-events: write`。
- 已同步项目索引、项目结构和变更日志。
- `pnpm check:text`、`pnpm lint:md`、`pnpm check:workspace`、`pnpm check:boundaries`、`pnpm check:guards` 和 `git diff --check` 均通过。

## 剩余问题

- 本地修改尚未提交和推送。GitHub Ubuntu Runner 的最终复验等待用户独立授权提交、推送后执行。
